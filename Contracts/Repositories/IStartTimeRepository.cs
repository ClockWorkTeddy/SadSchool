namespace SadSchool.Contracts.Repositories
{
    /// <summary>
    /// Represents a repository interface for managing start time entities.
    /// </summary>
    /// <remarks>This interface extends <see cref="IBaseRepository"/> to provide additional functionality
    /// specific to start time entities. Implementations should define methods for accessing and manipulating start time
    /// data.</remarks>
    public interface IStartTimeRepository : IBaseRepository
    {
    }
}
